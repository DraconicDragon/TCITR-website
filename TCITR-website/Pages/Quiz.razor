@page "/quiz"
@using System.Reflection.Metadata.Ecma335

<PageTitle>Quiz</PageTitle>

<h1>Test your knowledge!</h1>

<p role="status">Current count: @currentCount</p>

<label class="GroupTitle">Filters:</label><br/>
<input type="radio" name="number" id="all" checked="@checkedRadio("all")" @oninput="@(() => UpdateFilter("all"))" /> 


<button class="btn btn-primary" @onclick="EnterQuizAnswers">Enter Answers</button>



@code {
    private int currentCount = 0;


    private bool checkedRadio(string state)
    {
        if(state == "all")
        {
            return true;

        }
        else
        {
            return false;
        }
    }


    private void UpdateFilter(string option)
    {
        
    }



    private void EnterQuizAnswers()
    {
        currentCount++;
    }
}
